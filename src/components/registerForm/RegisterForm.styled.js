import styled from 'styled-components'

export const S = {
  ValidationErrorMessage: styled.span`
    color: ${({ color }) => color};
  `,
  Error: styled.p`
    color: ${({ color }) => color};
  `,
}
